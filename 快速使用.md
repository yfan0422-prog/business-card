# 🚀 快速使用指南

## 最简单的方式：**直接用浏览器！**

### 推荐方案一：浏览器访问（最简单）

```bash
# 终端运行
./start_http.sh
```

然后打开浏览器访问：**http://localhost:8000**

✅ 优点：
- 不需要处理证书问题
- 访问速度快
- 所有功能（除了摄像头）都能用

---

### 如果需要摄像头功能（拍照、人脸搜索）

```bash
# 终端运行
./start_https.sh
```

然后打开浏览器访问：**https://localhost:8443**

⚠️ 浏览器会提示"不安全"，点击"高级"→"继续访问"即可。

---

### 方案二：Electron桌面应用（可选）

#### 第一步：启动后端服务
```bash
# 新开一个终端
./start_http.sh
```

#### 第二步：启动Electron
```bash
# 在项目目录下
npm run electron
```

---

## 📱 功能说明

| 功能 | HTTP版本 | HTTPS版本 |
|------|----------|-----------|
| 查看名片 | ✅ | ✅ |
| 添加名片 | ✅ | ✅ |
| 编辑名片 | ✅ | ✅ |
| 搜索名片 | ✅ | ✅ |
| 生成名片卡片 | ✅ | ✅ |
| **拍照录入** | ❌ | ✅ |
| **人脸搜索** | ❌ | ✅ |

---

## 🎯 我的建议

1. **日常使用** → 用 `./start_http.sh` + 浏览器
2. **需要拍照/人脸搜索** → 用 `./start_https.sh` + 浏览器
3. **想试桌面App** → 用 HTTP版本 + Electron

---

## ❓ 常见问题

### Q: Electron 打不开？
A: 确保先启动了后端服务（`./start_http.sh`）！

### Q: 浏览器提示"不安全"？
A: 没关系，这是自签名证书导致的。点击"高级"→"继续访问"即可。

### Q: 摄像头用不了？
A: 请确保：
1. 使用 `./start_https.sh`（HTTPS版本）
2. 浏览器允许了摄像头权限

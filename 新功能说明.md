# 🎉 新增功能说明

## ✨ 人脸搜索功能

### 功能介绍
- 通过上传照片或拍照来搜索匹配的名片
- 使用AI大模型进行人脸比对
- 可调节置信度阈值
- 显示匹配度和判断理由

### 使用方法
1. 访问首页，找到「👤 人脸搜索」区域
2. 点击「📷 上传照片」选择图片，或点击「📸 拍照」直接拍摄
3. 调节「置信度阈值」滑块（默认0.6，范围0.3-0.95）
4. 点击「🔍 开始搜索」
5. 查看匹配结果

### 前提条件
- 需要先在「🤖 模型管理」中配置并启用AI模型
- 名片需要先录入「头像照片」才能被搜索到

---

## 💻 Electron桌面应用

### 功能介绍
- 将名片管理系统打包成桌面应用
- 像普通App一样使用，无需打开浏览器
- 支持Mac和Windows

### 使用方法

#### 方式一：直接运行（开发模式）
```bash
# 1. 先启动后端服务（需要新开一个终端）
./start_https.sh

# 2. 在另一个终端运行Electron
npm install  # 首次运行需要安装依赖
npm run electron
```

#### 方式二：打包成App
```bash
# 打包成可执行文件
npm run build

# 打包后的文件在 dist/ 目录
```

### 配置说明
在 `main.js` 中可以修改访问地址：
```javascript
const CONFIG = {
    targetUrl: 'https://localhost:8443',  // 本地HTTPS服务（推荐）
    // targetUrl: 'http://localhost:8000', // 本地HTTP服务（无摄像头）
    // targetUrl: 'https://businesscard.ai-codify.com', // 在线服务
};
```

---

## 🔐 HTTPS支持

### 为什么需要HTTPS？
- **摄像头功能强制要求**：现代浏览器的安全策略要求，只有HTTPS页面才能访问摄像头
- **人脸搜索、拍照录入**等功能都需要摄像头权限

### 两种启动方式

#### 1. HTTPS版本（推荐，支持摄像头）
```bash
./start_https.sh
```
- 访问地址：https://localhost:8443
- 支持拍照、人脸搜索等所有功能
- 浏览器会提示「不安全」（自签名证书），点击「继续访问」即可

#### 2. HTTP版本（仅本地浏览）
```bash
./start_http.sh
```
- 访问地址：http://localhost:8000
- 不支持摄像头相关功能
- 适合纯浏览、编辑使用

---

## 📱 手机使用（PWA）

### 添加到主屏幕
1. 用手机浏览器访问（需在同一WiFi）
2. 点击浏览器菜单 →「添加到主屏幕」
3. 之后就可以像App一样点击图标打开

### 注意事项
- iOS Safari 建议使用HTTPS版本
- Android Chrome 支持PWA安装体验更好

---

## 📋 完整功能列表

### ✅ 已有功能
- 名片录入（手动/拍照/OCR）
- 名片搜索（姓名/公司/全文）
- 名片卡片生成
- 公司组织架构展示
- AI模型管理
- Web管理界面

### 🆕 新增功能
- **人脸搜索** - 用照片找人
- **HTTPS支持** - 启用摄像头
- **Electron桌面App** - 桌面端使用
- **拍照优化** - 支持旋转、预览

---

## 🚀 快速开始

### 最简单的方式：启动HTTPS服务
```bash
cd /path/to/business-card
./start_https.sh
```

然后打开浏览器访问：https://localhost:8443

### 想试试桌面App？
```bash
# 终端1：启动后端
./start_https.sh

# 终端2：启动Electron
npm run electron
```

---

## 🔧 故障排除

### Q: 浏览器提示「不安全」？
A: 这是正常的，因为使用了自签名证书。点击「高级」→「继续访问」即可。

### Q: 摄像头用不了？
A: 请确保：
1. 使用的是HTTPS版本（start_https.sh）
2. 浏览器允许了摄像头权限
3. 电脑/手机有摄像头

### Q: 人脸搜索不到结果？
A: 请检查：
1. 名片是否录入了头像照片
2. 是否配置并启用了AI模型
3. 尝试调低置信度阈值
4. 确保照片清晰、人脸可见

### Q: Electron打不开？
A: 请确保：
1. 后端服务已先启动
2. 访问地址配置正确
3. npm install 已执行

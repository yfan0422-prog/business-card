# 群晖部署详细指南

## 📋 准备工作检查清单

- [x] 项目代码已完成
- [x] Docker已安装
- [x] 企业微信应用已创建
- [x] 配置信息已保存到.env
- [ ] 需要: 公网访问方式

---

## 📦 第一步：复制项目到群晖

### 方式A：通过文件共享（推荐）

1. **在Mac上打开Finder**
2. 按 `Cmd+K` 打开"连接服务器"
3. 输入：`smb://192.168.0.101`
4. 连接，选择一个共享文件夹（比如 `docker` 或 `home`）
5. 把整个 `business-card-system` 文件夹复制到群晖

### 方式B：通过Synology Drive
1. 在群晖上启用Synology Drive
2. 在Mac上安装Synology Drive客户端
3. 同步文件夹

---

## 🐳 第二步：在群晖上启动Docker

### 1. SSH登录群晖
在Mac终端执行：
```bash
ssh 你的群晖用户名@192.168.0.101
```

### 2. 进入项目目录
```bash
cd /path/to/business-card-system  # 替换为你复制到的实际路径
```

### 3. 启动Docker容器
```bash
sudo docker-compose up -d
```

### 4. 查看运行状态
```bash
sudo docker-compose ps
sudo docker-compose logs
```

---

## 🌐 第三步：配置公网访问（关键！）

企业微信需要**公网HTTPS地址**才能访问你的服务。

### 方案1：使用QuickConnect + 反向代理（如果你有QuickConnect）

#### 3.1 配置反向代理

1. 登录群晖管理后台：http://192.168.0.101:5000
2. 打开 **控制面板 → 应用程序门户 → 反向代理**
3. 点击 **新增**
4. 填写：

**来源（外网访问）：**
- 协议：`HTTPS`
- 主机名：`你的QuickConnect地址` (比如 `user123.synology.me`)
- 端口：`443`
- 路径：`/wechat`

**目标（内网服务）：**
- 协议：`HTTP`
- 主机名：`localhost`
- 端口：`8000`
- 路径：`/wechat`

5. 点击确定

---

### 方案2：如果你有公网IP和域名

#### 3.1 在路由器配置端口转发
- 把外网443端口转发到群晖的443端口

#### 3.2 配置反向代理（同方案1）

#### 3.3 申请免费HTTPS证书（Let's Encrypt）
1. 控制面板 → 安全性 → 证书
2. 新增 → 申请Let's Encrypt证书
3. 填写你的域名

---

## 🔗 第四步：完成企业微信配置

### 4.1 填写接收消息URL

回到企业微信应用设置的"接收消息"页面：

- **URL**: `https://你的公网地址/wechat`
- **Token**: `businesscard2024`
- **EncodingAESKey**: `uAduLaIXPkDkUmSabz8PiLvPPzByld1fNZw3NDbxEri`

点击 **保存**

### 4.2 配置可见范围

在应用详情页，找到"可见范围"，点击"设置可见范围"，添加你自己的账号。

---

## 📱 第五步：测试使用

1. 在手机上打开企业微信
2. 找到"名片管理助手"应用
3. 发送消息测试：
   - `帮助`
   - `列表`
   - 直接发一张图片试试录入

---

## 🔧 故障排查

### Docker无法启动
```bash
# 检查端口是否被占用
sudo netstat -tulpn | grep 8000

# 查看Docker日志
sudo docker-compose logs
```

### 企业微信验证失败
- 检查URL是否以 `https://` 开头
- 确认URL路径包含 `/wechat`
- 检查Token和EncodingAESKey是否正确
- 查看Docker容器日志

### 消息无响应
- 检查可见范围是否包含你的账号
- 查看Docker容器日志：`sudo docker-compose logs -f`

---

## 📂 目录结构（群晖上）

```
business-card-system/
├── app/                  # 应用代码
├── data/                # 数据存储（重要！备份这个）
│   ├── db/             # 数据库文件
│   ├── photos/         # 名片照片
│   └── avatars/        # 头像
├── fonts/              # 字体文件
├── .env                # 配置文件（重要！）
├── docker-compose.yml  # Docker配置
└── Dockerfile          # Docker镜像
```

---

## 💾 备份建议

定期备份 `data` 目录：
```bash
tar -czf business-card-backup-$(date +%Y%m%d).tar.gz data/
```

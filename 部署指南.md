# 名片管理系统 - 服务器部署与更新指南

## 服务器信息

| 项目 | 值 |
|------|-----|
| 服务器类型 | 阿里云轻量服务器 |
| 公网 IP | 106.15.180.105 |
| 登录用户 | root（或用 admin 登录后 `sudo su`） |
| 项目路径 | `/opt/business-card` |
| 数据路径 | `/opt/business-card-data`（**代码目录外，永久安全**） |
| 运行端口 | 8000 |
| 启动方式 | systemd 服务（开机自启） |

---

## 第一次部署（已完成，仅作参考）

### 1. 添加 GitHub SSH 密钥（一劳永逸）

```bash
ssh-keygen -t ed25519 -N "" -f ~/.ssh/id_ed25519
cat ~/.ssh/id_ed25519.pub
```

把输出的公钥添加到 https://github.com/settings/ssh/new（选 **Authentication Key**）

### 2. 克隆代码

```bash
cd /opt
git clone git@github.com:yfan0422-prog/business-card.git
cd business-card
```

### 3. 创建虚拟环境并安装依赖

```bash
# 先安装 python3-venv（仅首次需要）
apt install python3.12-venv -y

python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

### 4. 创建 .env 配置文件

```bash
cat > .env << 'EOF'
WECHAT_CORP_ID=your_corp_id_here
WECHAT_SECRET=your_secret_here
WECHAT_AGENT_ID=your_agent_id_here
WECHAT_TOKEN=your_token_here
WECHAT_ENCODING_AES_KEY=your_aes_key_here
DATA_DIR=/opt/business-card-data
HOST=0.0.0.0
PORT=8000
EOF
```

### 5. 创建独立数据目录（与代码隔离）

```bash
mkdir -p /opt/business-card-data/db /opt/business-card-data/photos /opt/business-card-data/avatars
```

> `.env` 中 `DATA_DIR` 必须指向 `/opt/business-card-data`，不是项目内的 `data/`。

### 6. 安装 systemd 服务（开机自启）

```bash
cp /opt/business-card/business-card.service /etc/systemd/system/
systemctl daemon-reload
systemctl enable business-card
systemctl start business-card
```

---

## ⚠️ 数据安全红线（每次更新前必读）

```
数据目录 /opt/business-card-data   → 代码目录 /opt/business-card
      ↑                                          ↑
   永久保留                                  可以随便删
  （数据库、照片、知识库）                    （纯代码，git管理）
```

### 🔴 绝对禁止

```bash
# ❌ 永远不要执行这个！会删掉所有数据！
rm -rf /opt/business-card
```

### 🟢 正确更新方式

```bash
# ✅ git pull 只更新代码，不影响数据目录
cd /opt/business-card && git pull && systemctl restart business-card
```

### 🔵 数据备份（可选，推荐每次更新前执行）

```bash
cp /opt/business-card-data/db/business_cards.db /opt/business-card-data/db/business_cards.db.bak.$(date +%Y%m%d_%H%M)
```

---

## ⭐ 后续更新（小白三步法）

**第一步：SSH 登录服务器**

在你的 Mac 终端执行：

```bash
ssh root@106.15.180.105
```

输入密码后回车。

> 如果你用的是 admin 账户，需要先 `sudo su` 切换到 root。

**第二步：更新代码并重启服务**

```bash
cd /opt/business-card && git pull && systemctl restart business-card
```

看到 `active (running)` 就说明更新成功。

**第三步：验证**

在浏览器访问 http://106.15.180.105:8000/，确认新功能生效。

> 因为可能有浏览器缓存，如果功能没更新按 `Cmd+Shift+R`（Mac）或 `Ctrl+Shift+R`（Windows）强制刷新。

---

## 常用管理命令

```bash
# 查看服务状态
systemctl status business-card

# 停止服务
systemctl stop business-card

# 启动服务
systemctl start business-card

# 重启服务
systemctl restart business-card

# 查看实时日志
journalctl -u business-card -f

# 查看最近日志
journalctl -u business-card -n 50

# 查看错误日志
journalctl -u business-card -p err
```

---

## 本次部署的经验教训

| 问题 | 原因 | 解决方案 |
|------|------|----------|
| 🔴 **数据全部丢失** | `rm -rf business-card` 删掉了项目内的 `data/` | 数据目录移到项目外 `/opt/business-card-data`，永不删除 |
| `git pull` 失败（TLS 错误） | 国内服务器访问 GitHub 被墙 | 改用 SSH 协议克隆（`git@github.com:...`） |
| `scp` 连接超时 | 阿里云安全组未开放 SSH 端口给外部 IP | 不用 scp，走 GitHub SSH |
| 找不到项目目录 | 之前部署在 `/opt/` 而非 `/root/` | 用 `find / -name "app" -type d \| grep business-card` 定位 |
| `python3-venv` 未安装 | 阿里云 Ubuntu 最小化安装 | `apt install python3.12-venv -y` |
| service 文件路径错误 | 旧文件写的是 `/root/business-card` | 已更新为 `/opt/business-card` |
| nohup 进程残留 | 手动启动后未停止 | 部署前 `pkill -f uvicorn` 清理 |
| vi/vim 编辑文件复杂 | 服务器无 GUI | 用 `cat > file << 'EOF'` 直接写入内容 |

### 核心原则

1. **SSH 密钥优先** — 一次性配置后 `git pull` 永久可用，比 HTTPS 可靠
2. **systemd 管理** — 不要用 `nohup` 手动启动，用 `systemctl` 管理生命周期和开机自启
3. **代码走 GitHub，数据留服务器** — `data/` 目录被 `.gitignore` 忽略，不会因 git 操作丢失
4. **部署文件三件套** — `business-card.service`（服务定义）、`start_prod.sh`（启动脚本）、`.env`（环境变量），缺一不可

---

## 故障排查速查表

| 现象 | 检查命令 | 常见原因 |
|------|----------|----------|
| 网站打不开 | `curl -sL http://localhost:8000/ \| head -5` | 服务没启动、端口被占 |
| 服务显示 failed | `systemctl status business-card` | .env 配置错误、venv 未安装依赖 |
| 端口不通 | `ss -tlnp \| grep 8000` | 服务没监听、防火墙拦截 |
| git pull 失败 | `ssh -T git@github.com` | SSH 密钥过期、网络不通 |
| 功能没更新 | 浏览器强制刷新 | 缓存了旧 HTML/JS |

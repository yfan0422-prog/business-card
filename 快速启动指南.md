# 名片管理系统 - 快速启动指南

## ✅ 当前状态
- [x] 项目已完整创建
- [x] 所有测试通过
- [x] 中文字体已配置
- [x] 测试卡片生成成功

## 📱 第一步：配置企业微信

### 1.1 注册企业微信
1. 访问 https://work.weixin.qq.com/
2. 点击"立即注册"（个人可免费注册）
3. 完成注册流程

### 1.2 获取企业ID (CORP_ID)
1. 登录管理后台
2. 点击"我的企业"
3. 找到"企业ID"并复制
4. 粘贴到 `.env` 文件的 `WECHAT_CORP_ID=` 后面

### 1.3 创建应用
1. 点击"应用管理" → "创建应用"
2. 填写：
   - 应用名称：名片管理助手
   - 介绍：个人名片管理
3. 创建成功后，在应用详情页获取：
   - **AgentId** → 填入 `WECHAT_AGENT_ID=`
   - **Secret** (点击查看) → 填入 `WECHAT_SECRET=`

### 1.4 设置Token和EncodingAESKey
1. 在应用详情页找到"接收消息"或"API接收"
2. 点击"设置接收消息"
3. 设置：
   - **Token**: 自己设置一个字符串，比如 `businesscard2024`
   - **EncodingAESKey**: 点击"随机获取"
4. 先**不要保存URL**，先完成部署

## 🚀 第二步：运行服务

### 方式一：本地直接运行（推荐先测试）

```bash
cd /Users/yfan/business-card-system
source venv/bin/activate
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

打开浏览器访问：http://localhost:8000/
看到"名片管理系统运行中"说明服务正常！

### 方式二：Docker运行（群晖部署用）

```bash
cd /Users/yfan/business-card-system
docker-compose up -d
```

## 🌐 第三步：配置公网访问

企业微信需要公网可访问的URL。推荐以下方式之一：

### 选项A：内网穿透工具（推荐先测试）
使用 ngrok、frp、花生壳等工具：
```bash
# 使用 ngrok 示例
ngrok http 8000
```
会得到一个类似 `https://xxx.ngrok.io` 的公网地址

### 选项B：群晖配置
1. 确保群晖有公网IP或使用QuickConnect
2. 在群晖配置反向代理
3. 使用HTTPS证书（Let's Encrypt免费）

## 🔗 第四步：完成企业微信配置

### 4.1 填写接收消息URL
回到企业微信应用设置的"接收消息"页面：
- **URL**: `https://你的公网域名/wechat`
- **Token**: 填入你设置的Token
- **EncodingAESKey**: 填入之前获取的Key
- **点击保存**

### 4.2 配置可见范围
在应用详情页的"可见范围"中，添加你自己的账号。

## 📲 第五步：开始使用

在企业微信中找到你创建的应用，发送以下消息测试：

| 命令 | 说明 |
|------|------|
| `帮助` | 查看帮助信息 |
| `列表` | 查看最近录入的名片 |
| `搜索 张三` | 搜索联系人 |
| `公司 某某科技` | 查看公司联系人 |
| 直接发照片 | 开始录入名片 |

## 📋 下一步：群晖部署

详细的群晖部署步骤请看 `DEPLOY.md` 文件。

## ⚠️ 注意事项

1. **不要提交 `.env` 文件到 git** - 已包含在 `.gitignore` 中
2. **数据存储在 `data/` 目录** - 定期备份此目录
3. **字体文件** - 已复制STHeiti字体到 `fonts/` 目录
4. **需要HTTPS** - 企业微信要求回调URL使用HTTPS

## 🆘 问题排查

### 服务无法启动
```bash
# 检查端口是否被占用
lsof -i :8000
# 检查虚拟环境
source venv/bin/activate && pip list
```

### 企业微信验证失败
- 检查URL是否正确（包含 /wechat 路径）
- 确认Token和EncodingAESKey匹配
- 查看服务日志
